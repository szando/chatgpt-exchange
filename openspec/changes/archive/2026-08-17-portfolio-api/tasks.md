# Tasks: Build Portfolio Demo API

All tasks in this archived baseline change are complete at the design/specification level. Implementation execution is handed off separately.

## Contract and generation

- [x] `TASK-BASE-001` Define the complete shallow business OpenAPI 3.0.3 surface.
- [x] `TASK-BASE-002` Define health and readiness OpenAPI operations.
- [x] `TASK-BASE-003` Assign stable operation, requirement, acceptance-criterion, and error identifiers.
- [x] `TASK-BASE-004` Define build-time request-schema and TypeScript-type generation.

## Persistence and fixtures

- [x] `TASK-BASE-005` Define the permissive SQLite schema and schema-version markers.
- [x] `TASK-BASE-006` Define deterministic sequence and transaction behavior.
- [x] `TASK-BASE-007` Define baseline JSON fixtures and fixed loading order.
- [x] `TASK-BASE-008` Define adverse fixture profiles and exact expected-issue comparison.
- [x] `TASK-BASE-009` Define fixture validation without database-enforced business constraints.

## Runtime behavior

- [x] `TASK-BASE-010` Define Fastify composition, startup, configuration, and shutdown.
- [x] `TASK-BASE-011` Define query filters and fixed ordering.
- [x] `TASK-BASE-012` Define allocation-order creation and retrieval.
- [x] `TASK-BASE-013` Define draft replacement, submission, and cancellation.
- [x] `TASK-BASE-014` Define reference, lifecycle, quantity, individual-percentage, and client-reference validation.
- [x] `TASK-BASE-015` Define Problem Details and stable status/error mappings.
- [x] `TASK-BASE-016` Define response defect-preservation and duplicate-ID behavior.

## Verification and delivery

- [x] `TASK-BASE-017` Define unit, repository, contract, adverse, lifecycle, and container verification layers.
- [x] `TASK-BASE-018` Define Windows and Linux/Node.js 24 compatibility expectations.
- [x] `TASK-BASE-019` Define offline and non-root container constraints.
- [x] `TASK-BASE-020` Publish the resulting baseline capability specifications under `openspec/specs`.

